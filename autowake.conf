# AutoWake schedule. Rerun install.sh after changing suspend settings so the
# systemd timer is regenerated.
SUSPEND_WEEKDAY=Fri
SUSPEND_TIME=19:00
WAKE_WEEKDAY=Mon
WAKE_TIME=06:00
RTC_DEVICE=/dev/rtc0
